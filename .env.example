# Port the local API server listens on
PORT=3000

# Where persistent browser profiles (logged-in sessions) are stored
PROFILES_DIR=./profiles

# Run browsers headless (true) or with a visible window (false).
# Keep this false while getting started so you can watch it work and so
# login sessions stay valid; flip to true once a provider is reliable.
HEADLESS=false
