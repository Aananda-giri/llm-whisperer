# Port the local API server listens on
PORT=3000

# Where persistent browser profiles (logged-in sessions) are stored
PROFILES_DIR=./profiles

# Run browsers with a visible window (true) or headless (false).
# Use HEADLESS=false for the one-time manual login of each service.
HEADLESS=true
